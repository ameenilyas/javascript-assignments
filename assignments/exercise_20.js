const favs = [];

function addFav(url) {
  const sport = "Cricket";
  const place = "Sydney";
  const language = "Arabic";

  favs.push(sport);
  favs.push(place);
  favs.push(language);
}

addFav();
console.log(favs);
